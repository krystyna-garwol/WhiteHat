<nav class="navbar navbar-expand-lg navbar-light">
  <a href="/">
    <i id="logo" class="fas fa-project-diagram fa-lg"></i>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active text-center">
        <a class="nav-link" href="/users/{{user.id}}/boards">ALL Boards</a>
      </li>
    </ul>
    <div class="nav-item text-center">
      <a href="/users/{{user.id}}/profilepage"><img class="avatar-nav" src={{user.image}}alt="Active user avatar"></a>
    </div>
  </div>
</nav>

<div class="container">
  <div class="userprofile-img-j" style="background-image: url({{user.image}});"></div>
  <div class="d-flex justify-content-center">
    <div class="col-sm-6">
      <div class="card" id="edit-user">
        <div class="card-header">Edit Your Details</div>
        <div class="card-body">
          <form action="/users/{{user.id}}/profilepage/edit" method="POST">
            <div class="form-group">
              <label>Name</label>
              <input type="text" name="name" value="{{user.name}}" class="form-control" required>
            </div>
            <lavel>Avatar</lavel>
            <input type="url" name="image" value="{{user.image}}" class="form-control"
              required>

            <div class="btn-user d-flex flex-wrap justify-content-center">
              <button type="submit" class="btn"><i class="fas fa-check"></i>&nbsp; Submit</button>
              <a href="/users/{{user.id}}/profilepage/delete"
                onclick="return confirm('Are you sure you want to delete this?')">
                <button type="button" class="btn"><i class="fas fa-times"></i>&nbsp; Delete</button></a>
              <a href="/">
                <button type="button" class="btn"><i class="fas fa-sign-out-alt"></i>&nbsp; Log out</button>
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>

  </div>
</div>